#本节学习地址:https://www.liaoxuefeng.com/wiki/1016959663602400/1017104324028448
#dict 存在key-value

#set  只存在key 不存在value

#可变对象与不变对象
#对于可变对象，比如list，对list进行操作，list内部的内容是会变化的
#对于不变对象来说，调用对象自身的任意方法，也不会改变该对象自身的内容。
#相反，这些方法会创建新的对象并返回，这样，就保证了不可变对象本身永远是不可变的。
#python 创建空集合
num = set()